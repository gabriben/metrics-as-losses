
# def split(X, y, s, r):
#     i = np.arange(len(X.index))
#     np.random.shuffle(i)





    
#   X_train, X_val, y_train, y_val = train_test_split(movies['imdbId'], movies['Genre'], test_size=0.2, random_state=r)


#   print("Number of posters for training: ", len(X_train))
#   print("Number of posters for validation: ", len(X_val))


# import numpy as np
# X = np.arange(103)
# i = np.arange(103)
# s = [0.2, 0.2, 0.6]
# np.random.shuffle(i)
# P = np.rint(np.multiply(i.size, s))
# P[-1] = 103 - np.sum(p[:-1])
# P = np.cumsum(P)
# P = P - 1

# last = -1
# for j, p in enumerate(P): 
#     X[last]

# if np.sum(s) == 1.0:
    
